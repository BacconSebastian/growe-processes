{
  "name": "growe-cron-scheduler",
  "version": "1.0.0",
  "description": "Scheduler HTTP delgado que dispara los cron jobs de Growe vía POST al backend.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "node-cron": "^3.0.3"
  }
}
